services:
    ALAS:
        container_name: "alas"
        image: "alas:py314"
        build:
            context: .
            dockerfile: ./deploy/docker/Dockerfile
            # dockerfile: ./deploy/docker/Dockerfile.cn
            args:
                UV_INDEX_URL: ${UV_INDEX_URL:-https://pypi.org/simple}
                UV_EXTRA_INDEX_URL: ${UV_EXTRA_INDEX_URL:-}
        network_mode: host
        restart: unless-stopped
        init: true
        working_dir: /app/AzurPilot
        environment:
            TZ: ${TZ:-Asia/Shanghai}
            PYTHONDONTWRITEBYTECODE: "1"
            PYTHONUNBUFFERED: "1"
        volumes:
            - '.:/app/AzurPilot:rw'
            - 'alas-venv:/app/AzurPilot/.venv'
            # - '../MAA:/app/MAA:rw'
            - '/etc/localtime:/etc/localtime:ro'
        command: [".venv/bin/python", "gui.py"]
        healthcheck:
            test:
                [
                    "CMD-SHELL",
                    ".venv/bin/python -c \"import socket; s=socket.create_connection(('127.0.0.1', 25548), 3); s.close()\"",
                ]
            interval: 30s
            timeout: 5s
            retries: 6
            start_period: 90s

volumes:
    alas-venv:
